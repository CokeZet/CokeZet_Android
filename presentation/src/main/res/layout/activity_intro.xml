<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="buy.coke.zet.common.designsystem.statebutton.LogoButton.BrandName" />
        <import type="buy.coke.zet.common.designsystem.topbar.TopBar.Image"/>
        <import type="buy.coke.zet.common.designsystem.normalbutton.NormalButton.Color"/>
        <import type="buy.coke.zet.common.designsystem.normalbutton.NormalButton.Shape"/>

        <variable
            name="viewModel"
            type="buy.coke.zet.presentation.IntroViewModel" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".IntroActivity">
        
        <buy.coke.zet.common.designsystem.topbar.TopBar
            android:id="@+id/topbar"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            app:leftIconImage="@{Image.LOGO}"
            app:leftText="@{@string/text}"
            app:rightFirstIconImage="@{Image.NOTIFICATION}"
            app:rightSecondIconImage="@{Image.GRAPH}"
            app:rightThirdIconImage="@{Image.PERSON}"
            app:layout_constraintTop_toTopOf="parent"/>

        <buy.coke.zet.common.designsystem.statebutton.LogoButton
            android:id="@+id/logo_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:brand="@{BrandName.BC}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <buy.coke.zet.common.designsystem.statebutton.SwitchButton
            android:id="@+id/switch_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="10dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/logo_button"/>

        <buy.coke.zet.common.designsystem.normalbutton.NormalButton
            android:id="@+id/rectangle_button"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginStart="25dp"
            android:layout_marginEnd="25dp"
            android:layout_marginTop="10dp"
            app:mainColor="@{Color.LIGHT_RED}"
            app:buttonText="@{@string/button}"
            app:buttonShape="@{Shape.RECTANGLE}"
            app:clickListener="@{() -> viewModel.rotateDesign()}"
            app:layout_constraintTop_toBottomOf="@id/switch_button"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <buy.coke.zet.common.designsystem.normalbutton.NormalButton
            android:id="@+id/round_button"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginStart="25dp"
            android:layout_marginEnd="25dp"
            android:layout_marginTop="10dp"
            app:mainColor="@{Color.LIGHT_RED}"
            app:buttonText="@{@string/button}"
            app:buttonShape="@{Shape.ROUND}"
            app:layout_constraintTop_toBottomOf="@id/rectangle_button"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>